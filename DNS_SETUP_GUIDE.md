# 🌐 Руководство по настройке DNS для alexautozakup.kz

## ❗ Важно: Разница между NS и A-записью

### Name-серверы (NS) - это НЕ то же самое, что A-запись!

**Name-серверы (NS)** указывают на DNS-серверы, которые обрабатывают DNS-запросы для вашего домена.  
**A-запись** указывает домен на IP-адрес вашего веб-сервера.

## ✅ Что нужно настроить

### 1. Name-серверы (NS) - УЖЕ НАСТРОЕНО

Если вы видите форму с NS записями:
- **NS1**: `85.198.89.34` (уже заполнено)
- **NS2**: можно оставить пустым или добавить второй (не обязательно)

**Важно:** NS записи могут указывать на DNS-серверы, а не обязательно на IP вашего сервера. Обычно провайдер предоставляет свои NS серверы (например, `ns1.provider.com`, `ns2.provider.com`).

### 2. A-запись - ЭТО ГЛАВНОЕ! ⚠️

Нужно найти раздел **"A-записи"** или **"DNS записи"** и настроить:

**A-запись для основного домена:**
- **Имя/Хост**: `@` (или `alexautozakup.kz` или оставить пустым)
- **Тип**: `A`
- **Значение/IP**: `85.198.89.34`
- **TTL**: `3600` (или минимальное значение)

**A-запись для www поддомена:**
- **Имя/Хост**: `www`
- **Тип**: `A`
- **Значение/IP**: `85.198.89.34`
- **TTL**: `3600`

## 📋 Где искать настройку A-записей?

В зависимости от панели управления хостингом:

### Вариант 1: Панель управления доменом
Ищите разделы:
- **"DNS записи"**
- **"DNS зона"**
- **"Записи домена"**
- **"Управление DNS"**
- **"Редактирование DNS"**

### Вариант 2: Часто A-записи находятся рядом с NS
После настройки Name-серверов должна быть кнопка:
- **"Управление DNS-записями"**
- **"Добавить запись"**
- **"Создать A-запись"**

## 🔍 Что нужно сделать СЕЙЧАС

1. **Сохраните NS записи** (если еще не сохранили):
   - Нажмите кнопку **"Сохранить"** в форме Name-серверов

2. **Найдите раздел с A-записями**:
   - Ищите вкладку **"DNS записи"** или **"Управление DNS"**
   - Или кнопку **"Добавить запись"** / **"Создать запись"**

3. **Создайте A-запись**:
   ```
   Тип: A
   Имя: @ (или alexautozakup.kz)
   Значение: 85.198.89.34
   TTL: 3600
   ```

4. **Создайте A-запись для www** (опционально, но рекомендуется):
   ```
   Тип: A
   Имя: www
   Значение: 85.198.89.34
   TTL: 3600
   ```

## ✅ Проверка после настройки

После сохранения DNS записей проверьте:

```bash
cd /root/alex
./check-dns.sh
```

Ожидаемый результат:
```
✅ DNS запись корректна: 85.198.89.34
```

## ⏱️ Время распространения DNS

После сохранения изменений:
- **Обычно**: 5-30 минут
- **Максимум**: 4-24 часа (как указано в предупреждении)

## 🚀 После настройки DNS

Когда DNS записи обновятся:

1. **Запустить проект**:
   ```bash
   cd /root/alex
   docker-compose up -d
   ```

2. **Получить SSL сертификат** (см. `DOMAIN_SETUP.md`)

3. **Переключить на HTTPS** (см. `DOMAIN_SETUP.md`)

## ❓ Если не можете найти настройку A-записей

1. Проверьте другие разделы панели управления:
   - "DNS"
   - "Домены"
   - "Управление доменом"
   - "DNS зона"

2. Ищите кнопки:
   - "Добавить запись"
   - "Создать запись"
   - "Управление записями"

3. Свяжитесь с поддержкой хостинга:
   - Уточните, где настраиваются A-записи для домена

## 📝 Итоговый чеклист

- [ ] Сохранены Name-серверы (NS)
- [ ] Найдена секция A-записей
- [ ] Создана A-запись для основного домена (`@` → `85.198.89.34`)
- [ ] Создана A-запись для www (`www` → `85.198.89.34`)
- [ ] Проверка DNS через `./check-dns.sh`
- [ ] DNS записи обновились (положительный результат проверки)

## 🔗 Связанные файлы

- `QUICK_START.md` - Быстрый запуск проекта
- `DOMAIN_SETUP.md` - Настройка домена и SSL
- `check-dns.sh` - Скрипт проверки DNS

