# 🐳 Инструкция по запуску проекта через Docker

## Быстрый старт

### 1. Проверка Docker

Убедитесь, что Docker и Docker Compose установлены:

```bash
docker --version
docker-compose --version
```

### 2. Настройка переменных окружения

Файл `.env` уже создан на основе `.env.example`. Проверьте и при необходимости обновите:

```bash
# Основные настройки уже настроены для Docker
# DATABASE_URL указывает на контейнер PostgreSQL
# Для работы поиска поставщиков нужно настроить:
# - GOOGLE_API_KEY
# - GOOGLE_SEARCH_ENGINE_ID
# См. GOOGLE_API_SETUP.md для подробностей
```

### 3. Запуск проекта

```bash
# Перейти в директорию проекта
cd /root/alex

# Запустить все сервисы
docker-compose up -d

# Просмотр логов
docker-compose logs -f app

# Или просмотр логов PostgreSQL
docker-compose logs -f postgres
```

### 4. Проверка работы

После запуска приложение будет доступно по адресу:
- **Приложение**: http://localhost:3000
- **PostgreSQL**: localhost:5432

### 5. Инициализация базы данных

База данных автоматически инициализируется при первом запуске:
- Схема БД применяется автоматически
- Начальные данные заполняются (если скрипт seed.ts существует)

Если нужно вручную выполнить миграции:

```bash
# Войти в контейнер приложения
docker-compose exec app sh

# Выполнить команды внутри контейнера
npx prisma db push
npx prisma generate
npm run db:seed
```

## Полезные команды

### Остановка сервисов
```bash
docker-compose down
```

### Пересборка контейнера
```bash
docker-compose build --no-cache
docker-compose up -d
```

### Просмотр статуса контейнеров
```bash
docker-compose ps
```

### Очистка данных БД (ОСТОРОЖНО!)
```bash
docker-compose down -v  # Удалит все volumes включая данные БД
```

### Доступ к базе данных
```bash
# Через psql внутри контейнера
docker-compose exec postgres psql -U alex_user -d alex_db

# Или через Prisma Studio (в контейнере приложения)
docker-compose exec app npm run db:studio
# Затем откройте порт: docker-compose exec app -- -p 5555:5555
```

### Просмотр логов
```bash
# Все сервисы
docker-compose logs

# Только приложение
docker-compose logs app

# Только PostgreSQL
docker-compose logs postgres

# В реальном времени
docker-compose logs -f app
```

## Структура Docker-файлов

- `Dockerfile` - образ для Next.js приложения
- `docker-compose.yml` - конфигурация всех сервисов
- `docker-entrypoint.sh` - скрипт инициализации БД при запуске
- `.dockerignore` - файлы, исключенные из образа

## Настройка переменных окружения

Основные переменные в `.env`:
- `DATABASE_URL` - уже настроен для Docker
- `JWT_SECRET` - сгенерирован автоматически
- `GOOGLE_API_KEY` - нужно получить (см. GOOGLE_API_SETUP.md)
- `GOOGLE_SEARCH_ENGINE_ID` - нужно получить (см. GOOGLE_API_SETUP.md)

## Решение проблем

### Контейнер не запускается
```bash
# Проверьте логи
docker-compose logs app

# Проверьте статус
docker-compose ps
```

### Ошибка подключения к БД
```bash
# Убедитесь, что PostgreSQL запущен
docker-compose ps postgres

# Проверьте healthcheck
docker-compose exec postgres pg_isready -U alex_user
```

### Нужно пересобрать образ
```bash
docker-compose build --no-cache app
docker-compose up -d
```

### Проблемы с Prisma
```bash
# Перегенерация Prisma Client
docker-compose exec app npx prisma generate

# Применение схемы заново
docker-compose exec app npx prisma db push
```

## Production

Для production рекомендуется:
1. Изменить пароли в `docker-compose.yml`
2. Использовать секреты Docker или внешний менеджер секретов
3. Настроить SSL/TLS
4. Использовать reverse proxy (nginx, traefik)
5. Настроить мониторинг и логирование

