# 🔑 Настройка Google Custom Search API

## Проблема
```
❌ Google API error 400: API key not valid. Please pass a valid API key.
```

Система не может выполнять поиск, потому что API ключи Google не настроены.

## ⚠️ ВАЖНО: Текущие ключи в коде НЕ работают!

В файле `app/api/requests/[id]/search/route.ts` есть дефолтные ключи, но они невалидные:
```javascript
const GOOGLE_API_KEY = process.env.GOOGLE_API_KEY || 'AIzaSyAtgzKN9vREtdLZuwqQlmSszVKU3DT1Qy8'
const SEARCH_ENGINE_ID = process.env.GOOGLE_SEARCH_ENGINE_ID || 'd7065ea5c59764932'
```

## 📋 Инструкция по настройке

### Шаг 1: Получить Google API ключ

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)

2. Создайте новый проект:
   - Нажмите на селектор проектов вверху
   - "New Project" → Имя: `alex-search-suppliers`
   - Нажмите "Create"

3. Включите Custom Search API:
   - В меню слева: "APIs & Services" → "Library"
   - Найдите "Custom Search API"
   - Нажмите "Enable"

4. Создайте API ключ:
   - "APIs & Services" → "Credentials"
   - "Create Credentials" → "API Key"
   - Скопируйте созданный ключ
   - (Опционально) "Restrict Key" → выберите "Custom Search API"

### Шаг 2: Создать Custom Search Engine

1. Откройте [Programmable Search Engine](https://programmablesearchengine.google.com/controlpanel/all)

2. Нажмите "Add" (Добавить)

3. Базовые настройки:
   - **Name**: `Alex Suppliers Search`
   - **What to search**: Выберите "Search the entire web"
   - Нажмите "Create"

4. Настройки (Settings):
   ```
   Search features:
   ✅ Image search: OFF
   ✅ SafeSearch: OFF
   ✅ Speech input: OFF
   
   Advanced:
   Region: Kazakhstan (если есть опция)
   Interface language: Russian
   ```

5. Скопируйте **Search engine ID** (cx):
   - Он будет выглядеть примерно так: `d7065ea5c59764932`
   - Находится в разделе "Basics" → "Search engine ID"

### Шаг 3: Обновить .env файл

Откройте файл `.env` и замените:

```bash
# API Keys
GOOGLE_API_KEY=AIza...ваш_настоящий_ключ_здесь
GOOGLE_SEARCH_ENGINE_ID=ваш_search_engine_id_здесь
```

**Пример:**
```bash
GOOGLE_API_KEY=AIzaSyBxYz123456789abcdefghijklmnopqr
GOOGLE_SEARCH_ENGINE_ID=a1b2c3d4e5f6g7h8i
```

### Шаг 4: Перезапустить сервер

```bash
# Остановите текущий процесс (Ctrl+C)
# Запустите снова
npm run dev
```

### Шаг 5: Протестировать

1. Создайте заявку с позицией "накидные ключи"
2. Нажмите "Автоматический поиск поставщиков"
3. Проверьте логи - должны быть результаты вместо ошибок

## 💰 Стоимость

Google Custom Search API:
- **Бесплатно**: 100 запросов в день
- **Платно**: $5 за 1000 запросов после лимита

При 21 запросе на заявку:
- Бесплатно можно обработать ~4-5 заявок в день
- Или оплатить расширенный лимит

## 🔧 Оптимизация количества запросов

Если нужно снизить количество запросов (сейчас 21 на заявку):

### Вариант 1: Уменьшить количество поисковых запросов

В `app/api/requests/[id]/search/route.ts` (строка 18-29):

```javascript
function generateSearchQueries(productName: string): string[] {
  const query = productName.trim();
  
  // Было: 5 вариантов → можно сократить до 3
  return [
    `купить ${query} Алматы`,
    `${query} интернет магазин Алматы`,
    `${query} оптом Алматы поставщик`,
    // Убрать эти 2:
    // `${query} цена Алматы`,
    // `${query} продажа Алматы`,
  ];
}
```

### Вариант 2: Убрать запросы по описанию

В той же функции (строка 272-279):

```javascript
// Закомментировать эти строки:
// if (position.description && position.description !== position.name) {
//   const descQueries = generateSearchQueries(position.description).slice(0, 2)
//   descQueries.forEach(q => searchQueriesWithProduct.push({ 
//     query: q, 
//     productName: position.name 
//   }))
// }
```

Это сократит запросы с 21 до ~9 на заявку (3 позиции × 3 запроса).

## 🆘 Troubleshooting

### "API key not valid"
- Проверьте, что ключ скопирован полностью
- Убедитесь, что Custom Search API включен в проекте
- Проверьте ограничения ключа (если есть)

### "Request limit exceeded"
- Достигнут лимит 100 запросов в день
- Подождите до следующего дня или оплатите расширенный лимит

### "Invalid search engine ID"
- Проверьте, что Search Engine ID скопирован правильно
- Убедитесь, что движок включен ("Search the entire web")

## 📚 Полезные ссылки

- [Google Custom Search JSON API](https://developers.google.com/custom-search/v1/overview)
- [Pricing](https://developers.google.com/custom-search/v1/overview#pricing)
- [Programmable Search Engine Help](https://support.google.com/programmable-search/)

---

После настройки ключей система начнёт находить релевантные магазины инструментов! 🎉

