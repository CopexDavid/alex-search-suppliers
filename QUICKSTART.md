# ⚡ Быстрый старт - ALEX

## 1️⃣ Установка (5 минут)

```bash
# Клонируйте репозиторий (если еще не сделано)
cd alex-search-suppliers

# Установите зависимости
npm install

# Скопируйте и настройте .env (уже создан с дефолтными значениями)
# При необходимости отредактируйте DATABASE_URL в файле .env
```

## 2️⃣ Настройка PostgreSQL

### Вариант А: Быстрая локальная установка

```bash
# macOS (через Homebrew)
brew install postgresql@15
brew services start postgresql@15

# Создайте базу данных
createdb alex_db
```

### Вариант Б: Docker (рекомендуется)

```bash
# Запустите PostgreSQL в Docker
docker run --name alex-postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=alex_db \
  -p 5432:5432 \
  -d postgres:15

# DATABASE_URL в .env уже настроен для этого варианта
```

## 3️⃣ Инициализация базы данных

```bash
# Примените схему базы данных
npm run db:push

# Сгенерируйте Prisma Client
npm run db:generate

# Заполните начальными данными (пользователи, шаблоны, настройки)
npm run db:seed
```

## 4️⃣ Запуск

```bash
# Запустите приложение
npm run dev
```

Откройте в браузере: http://localhost:3000

## 🔐 Вход в систему

Войдите с учетными данными:

**Администратор:**
- Email: `admin@alex.kz`
- Пароль: `Admin123!`

**Другие тестовые пользователи:**
- Закупщик: `purchaser@alex.kz` / `Test123!`
- Руководитель: `manager@alex.kz` / `Test123!`
- Наблюдатель: `viewer@alex.kz` / `Test123!`

## ✅ Готово!

Вы успешно запустили систему ALEX. Теперь можете:
- ✅ Авторизоваться с разными ролями
- ✅ Видеть разные разделы меню в зависимости от роли
- ✅ Выходить из системы
- 📋 Создавать заявки (функция в разработке)
- 🔍 Искать поставщиков (функция в разработке)

## 🛠️ Полезные команды

```bash
# Просмотр базы данных через UI
npm run db:studio

# Проверка кода
npm run lint

# Сброс базы данных
npm run db:reset
```

## 📖 Подробная документация

- [SETUP.md](./SETUP.md) - Полная инструкция по установке
- [API.md](./API.md) - Документация API
- [README.md](./README.md) - Техническое задание

## 🐛 Проблемы?

### "Cannot connect to database"
Убедитесь, что PostgreSQL запущен:
```bash
# macOS
brew services list

# Docker
docker ps | grep postgres
```

### "Prisma Client not found"
Выполните:
```bash
npm run db:generate
```

### Страница не загружается
Проверьте, что приложение запущено на http://localhost:3000

---

**Следующий шаг:** Начните разработку функционала управления заявками! 🚀

