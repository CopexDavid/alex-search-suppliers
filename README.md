Техническое задание на разработку веб-сервиса “Автоматизация закупок
ALEX”
Платформа, которая автоматизирует процесс закупок: от загрузки заявки из 1С до получения и сравнения
коммерческих предложений от поставщиков. Система берёт на себя рутинную работу: поиск поставщиков, рассылку
запросов, сбор и обработку документов, а пользователю остаётся только согласовать результат.

Паспорт системы:
Архитектура
●
●
●
●
●
●
Фронтенд: веб-интерфейс (Next.js, Tailwind), доступ через браузер.
Бэкенд: микросервисная архитектура на FastAPI (Python).
База данных: PostgreSQL (хранение заявок, поставщиков, КП).
AI-модули: парсинг коммерческих предложений (PDF/Word) и генерация поисковых запросов.
Интеграции: 1С (импорт/экспорт Excel), Email SMTP, WhatsApp Web (MVP).
Производительность и масштаб
●
●
●
Максимальная пропускная способность: 1000 заявок/месяц.
Средняя нагрузка: ~34 заявки в день, пиковая: до 100 заявок/сутки.
Одновременная обработка: до 100 активных заявок.
Основные функции
1. 2. 3. 4. 5. 6. 7. Импорт заявок из 1С и ручная загрузка.
Поиск поставщиков и сбор контактов.
Автоматическая рассылка запросов (WhatsApp, Email).
Приём и парсинг ответов от поставщиков.
Сравнение и визуализация коммерческих предложений.
Согласование и экспорт результатов обратно в 1С.
Отчётность и аналитика по заявкам.
Безопасность и доступ
●
●
●
Авторизация через учетные записи (JWT).
Ролевая модель доступа (Администратор, Закупщик, Руководитель, Просмотр).
Инфраструктура
●
●
●

Резервное копирование: ежедневные бэкапы БД и файлов.
Зачем это нужно
●
●
●
●
Скорость — заявки обрабатываются быстрее, чем вручную.
Экономия — система автоматически находит больше поставщиков, повышая конкуренцию и снижая цены.
Прозрачность — все этапы заявки зафиксированы и доступны в системе.
Минимум ошибок — AI-парсер корректно извлекает данные из документов, снижая риск человеческого
фактора.
●
Интеграция — результаты можно вернуть обратно в 1С в привычном формате Excel.
Как это работает (простыми словами)
1. 2. 3. 4. 5. 6. Сотрудник загружает заявку в систему (из 1С или вручную).
ИИ-сервис ищет подходящих поставщиков и отправляет им запросы на коммерческое предложение.
Поставщики присылают документы (PDF/Word), которые система автоматически анализирует.
Все предложения собираются в удобную таблицу для сравнения (цена, сроки, условия).
Ответственный сотрудник выбирает лучшее предложение и подтверждает закупку.
Результат возвращается в 1С и сохраняется в отчётах.
Результат для компании
●
●
●
●
Снижение затрат на закупки за счёт автоматизации и увеличения конкуренции между поставщиками.
Повышение прозрачности и управляемости закупочных процессов.
Сокращение времени от заявки до выбора поставщика.
У добная отчётность для руководства.
1. Документы-источники
Базовый черновик — Техническое Задание AI-Zakup.pdf (версия 1.0, 01.07.2025). Все основные разделы из него сохранены и
расширены.
2. Цель проекта
Автоматизировать и ускорить закупочный цикл, снизить операционные затраты и человеческие ошибки, обеспечить
прозрачность и хранение данных для аналитики.
3. Ограничение по пропускной способности
Система проектируется и тестируется под нагрузку не более 1000 заявок в месяц.
Последствия и требования:
●
●
●
●
Проектирование инфраструктуры и SLA ориентируется на среднюю ~34 заявки/день и пиковую нагрузку ≈ 50–100
заявок в сутки не чаще 3 дней в месяц.
Пиковая параллельная обработка (одновременное выполнение поисков/парсинга/отправки) — не более 10
параллельных задач.
Очередь задач (task queue) должна обеспечивать приоритетную обработку: критические заявки (deadline < 48 ч) —
повышенный приоритет.
При приближении к лимиту (на уровне ~90% месячной квоты) система должна генерировать предупреждение
администраторам и владельцу процесса.
4. Область охвата (что входит / не входит)
(Согласно исходному ТЗ, дополнено деталями)
Входит:
●
●
●
●
●
Импорт заявок из 1С (Excel/PDF) — ручной или автоматический загрузочный модуль.
Поиск поставщиков: внешний поиск (SerpAPI/Scraper) + внутренняя БД поставщиков.
Рассылка запросов КП через WhatsApp (WhatsApp Web JS) и Email (SMTP).
Приём и парсинг КП (PDF/Word), извлечение price/delivery/payment/company.
Сравнение КП, UI для согласования инициатором, экспорт в 1С (Excel).
Не входит (этап 2+):
●
Автоматизация оплат, генерация доверенностей, автокоррекция текстов договоров (опционально на этапе 2).
5. Бизнес-процесс (детально)
1. Пользователь создаёт заявку в 1С BitFinance → выгрузка Excel/PDF с мета-данными.
2. Пользователь загружает файл в UI (или потоком по API). UI создаёт запись request.
3. Микросервис search-engine генерирует поисковые запросы по каждой позиции: <позиция> купить Казахстан +
альтернативы.
4. contact-crawler сканирует топ-URL, извлекает телефоны, email, WhatsApp-ссылки.
5. WhatsApp-сервер / Email рассылает шаблон запроса КП поставщикам. Ответы принимаются (файлы/сообщения).
6. AI-парсер извлекает ключевые поля; данные сохраняются в quotes.
7. Алгоритм сравнения формирует таблицу, выделяет минимальную цену, учитывает дедлайн и срок поставки.
8. Инициатор видит таблицу, просматривает чат-логи, нажимает Подтвердить → экспорт Excel и запись в 1С.
6. Функциональные требования (развёрнуто)
Ниже — функциональные блоки с подробностями и API.
🧩 Функциональные требования к системе “ALEX — Автоматизация закупок”
F1. Управление заявками

Импорт заявок

Поддерживаемые форматы: .xlsx, .pdf (шаблон из 1С).

Возможность загрузки через UI или API.

Проверка обязательных полей (request_id, positions, deadline, budget).

Отображение статуса загрузки в интерфейсе.

Просмотр и редактирование заявок

Список всех заявок с фильтрацией по статусу, дате, инициатору.

Просмотр детальной информации по каждой заявке.

Изменение статуса вручную (только для роли “Администратор”).

Статус-машина

Возможные статусы: uploaded, in_search, quotes_received, on_review, approved, rejected, archived.

Автоматическое переключение статусов при выполнении действий (поиск, получение КП, подтверждение и т.д.).

F2. Поиск и управление поставщиками

Поиск поставщиков

Автоматическая генерация поисковых запросов по позициям из заявки.

Использование внешнего API (SerpAPI / Scraper) для получения ссылок.

Фильтрация по регионам (Казахстан / города).

Извлечение контактов

Автоматическое извлечение email, телефонов, WhatsApp-ссылок со страниц найденных компаний.

Сохранение контактов в базу данных с атрибутом достоверности (confidence score).

Справочник поставщиков

Хранение реквизитов: название, ИНН, контакты, рейтинг, действующие контракты.

Возможность ручного добавления / редактирования.

Поддержка тегов и рейтинга поставщиков.

F3. Отправка запросов на коммерческое предложение (КП)

Каналы отправки

WhatsApp (через WhatsApp Web JS — MVP).

Email (через SMTP, с очередями и повторными попытками).

Шаблоны сообщений

Поддержка динамических переменных: {request_id}, {position}, {deadline}.

Возможность редактирования шаблонов в панели администратора.

Мониторинг рассылки

Отображение количества успешно отправленных запросов.

Логи по каждому поставщику.

F4. Приём и обработка ответов (КП)

Приём файлов

Поддержка форматов .pdf, .docx, .zip.

Автоматическое сохранение в хранилище S3 (путь: /quotes/{request_id}/{supplier_id}).

Парсинг коммерческих предложений

Извлечение данных: price, currency, delivery_term, payment_term, company.

Использование AI-парсера (PyMuPDF + GPT).

Отметка о необходимости ручной проверки при неудачном парсинге.

Хранение истории

Сохранение оригиналов файлов и результатов парсинга.

Возможность скачать оригинал КП через UI.

F5. Сравнение и выбор предложения

Формирование таблицы сравнения

Автоматическое сравнение предложений по цене, срокам, условиям.

Взвешивание критериев:

Цена — 70%

Срок поставки — 20%

Рейтинг поставщика — 10%

Параметры можно изменять в админ-панели.

Интерфейс сравнения

Таблица минимум из трёх предложений.

Подсветка лучшего варианта.

Фильтры: по цене, сроку, рейтингу.

Согласование

Кнопка “Подтвердить” с фиксацией решения.

Возможность прикрепить комментарий при отклонении.

Изменение статуса на approved / rejected.

F6. Экспорт и интеграции

Экспорт результатов

Формирование итогового Excel-файла в формате 1С.

Возможность скачивания вручную или автоматической отправки через webhook.

Интеграция с 1С

Получение исходных заявок (Excel/PDF).

Возврат результатов закупки (выбранный поставщик, цена, КП-файлы).

Email / WhatsApp-интеграции

Для приёма и отправки сообщений в рамках заявок.

Хранение истории переписки в логах.

F7. Аналитика и отчётность

Отчёт по закупкам

Отображение метрик:

Количество заявок за период.

Средняя цена закупки.

Процент утверждённых заявок.

Визуализация данных (графики, таблицы).

Экспорт отчётов

В формате .csv или .xlsx.

С возможностью выбора периода.

Мониторинг эффективности

Отображение экономии средств.

Среднее время от заявки до утверждения.

F8. Управление пользователями и безопасностью

Авторизация и роли

JWT-аутентификация.

Роли:

Администратор — полный доступ.

Закупщик — создание, поиск, отправка запросов.

Руководитель — просмотр и утверждение.

Просмотр — только чтение.

Логи действий

Запись всех операций (кто что загрузил, одобрил, изменил).

Экспорт логов для аудита.

Шифрование данных

Все передаваемые и хранимые данные защищены TLS 1.3 / AES-256.

F9. Администрирование и технические функции

Очередь задач

Приоритизация критических заявок (deadline < 48ч).

Параллельная обработка не более 10 задач.

Уведомления

Уведомления администраторам при превышении лимита 1000 заявок/мес.

Предупреждения при ошибках парсинга или переполнении очередей.

Резервное копирование

Ежедневные бэкапы БД и S3-файлов.

Хранение не менее 14/30 дней соответственно.

F10. Отображение диалогов и коммуникация

История переписки

Просмотр WhatsApp-чатов с поставщиками (входящие/исходящие).

Возможность экспорта логов в .txt или .html.

Обратная связь

Возможность написать поставщику напрямую из интерфейса.

Система хранит все сообщения в контексте конкретной заявки.

💾 Итоговая структура функциональных модулей
Код	Модуль	Краткое описание
F1	Управление заявками	Импорт, статусы, просмотр
F2	Поиск поставщиков	Автоматический и ручной поиск
F3	Рассылка запросов	WhatsApp, Email, шаблоны
F4	Приём и парсинг КП	Хранилище, AI-парсер
F5	Сравнение и согласование	Алгоритм + UI
F6	Экспорт и интеграции	1С, Excel, webhooks
F7	Отчётность	Статистика, аналитика
F8	Безопасность	Роли, JWT, логи
F9	Системные процессы	Очереди, уведомления, бэкапы
F10	Коммуникации	Просмотр диалогов, сообщения
●
●
●
●
Форматы: .xlsx (шаблон), .pdf (сканы) + meta.json.
Валидация: обязательные поля — request_id, creator_id, positions[{sku, qty, unit, description}], deadline (ISO8601),
budget_currency.
API: POST /api/v1/requests/import — multipart/form-data: file (xlsx/pdf), meta (json).
Отклик: 201 + {"request_id": "<uuid>", "status":"uploaded"}.
F-02 UI загрузки и статус-машина
●
●
Статусы: uploaded, in_search, quotes_received, on_review, approved, rejected, archived.
UI: страница запроса с таймлайном статусов, активностью и ключевыми KPI.
F-03 Генерация поисковых запросов
●
●
Логика: для каждой позиции генерируется 3 варианта запроса (ключ/синоним/бренд).
Учитывать локализацию: +Казахстан, +Алматы при необходимости,Россия,(Китай-бета)
F-04 Search microservice
●
●
GET /search?query=...&limit=10 — возвращает top-10 URL с метаданными (title, snippet, country).
Использует SerpAPI / Scraper (с фоллбеком).
F-05 Contact crawler
●
●
Для каждого URL выполняет извлечение телефонов/email/whatsapp (регулярки + микроскрапинг).
Результат: list of contacts with type, value, confidence.
F-06 WhatsApp/Email sender
●
●
●
WhatsApp: интеграция через WhatsApp Web JS + Playwright для headless (MVP) или Business API на этапе 2.
Email: SMTP с очередями и retry (3 попытки, exponential backoff).
Шаблоны должны поддерживать переменные: {request_id}, {position}, {deadline}.
F-07 Приём входящих файлов
●
Все incoming files (pdf/docx/zip) сохраняются в S3: s3://quotes/<request_id>/<supplier_id>/<file>.
F-08 AI-парсер
●
●
Компоненты: PyMuPDF для извлечения текста + GPT-4o (function calling) для структуры.
Выход: {price: float, currency: "KZT", delivery_term: "10 days", payment_term:"30%
prepayment", company:"ООО..."}.
F-09 Алгоритм сравнения
●
●
Правила: минимальная цена + валидность поставки (delivery_term <= deadline).
Взвешивание: price (70%), delivery_term (20%), рейтинг поставщика (10%). Весируемые параметры настраиваемы в
админ-панели.
F-10 UI таблица сравнения
●
●
Показывать как минимум 3 КП; опция «показать ещё».
Фильтры: по сроку, цене, рейтингу, наличию контракта.
F-11 Просмотр диалогов
●
UI показывает чат-лог сообщений WhatsApp (входящие/исходящие) с возможностью экспортировать лог в
.txt/.html.
F-12 Подтверждение и экспорт
●
Нажатие Подтвердить → статус approved → скачивание request_<id>_approved.xlsx и webhook в 1С (если
настроено).
F-13 Ручной сценарий (без поиска)
●
Чек-бокс «без поиска»: пользователь вручную выбирает поставщика из справочника.
F-14 CRUD поставщиков
●
Поля: id, name, inn, address, phones[], emails[], whatsapp_link, rating, contract_valid_to, tags[].
F-15 Отчёт «Закупки за период»
●
Экспорт CSV/XLSX + визуализация графиков (кол-во заявок, средняя цена, % утверждённых).
7. Нефункциональные требования (детально)
●
●
●
●
●
●
Производительность (SLA для операций):
○
импорт заявки ≤ 3 с;
○
парсинг PDF ≤ 10 с;
○
генерация сравнительной таблицы ≤ 2 с.
Надёжность: MTBF ≥ 30 дней. Резервное копирование S3 — раз в 24 ч.
Безопасность: TLS1.3; OAuth2/JWT; RBAC (Admin / Purchaser / Initiator / Viewer); 
Масштабируемость: горизонтальное масштабирование контейнеров; отдельные сервисы (search/parser/wa) —
отдельные контейнеры.
Стабильность под лимит: тесты нагрузки ограничиваются 1000 заявок/мес; система должна корректно работать при
120% этого лимита в стресс-тесте (graceful degradation: очереди).
Локализация: интерфейс — русский, валюта/дата — KZT/формат DD.MM.YYYY.
8. Архитектура и стек (развёрнуто)
●
●
●
●
●
●
Frontend: Next.js 14, Tailwind, shadcn/ui (App Router).
API Gateway: FastAPI (Gunicorn + Uvicorn workers).
Бэкэнд-сервисы: Search Engine (Scrapy/Python), Contact Crawler, AI-Parser (Python + PyMuPDF + OpenAI), WhatsApp
Service (WhatsApp Web JS + Playwright), Email Sender.
DB: PostgreSQL 15 (Prisma ORM).
9. Модель данных (SQL / поля)
Привожу расширенную схему таблиц (ключевые поля и типы).

(Полная схема — в прикладной ER-диаграмме в приложении).
10. API спецификация (основные endpoints, примеры)
(JSON примеры сокращены)
1) Импорт заявки
POST /api/v1/requests/import
Request: multipart: file=xlsx, meta=json
Response: 201 { "request_id":"uuid", "status":"uploaded" }
2) Получить заявку
GET /api/v1/requests/{id}
Response: 200 { "id":"...", "status":"on_review", "positions":[...], "quotes":[...] }
3) Запустить поиск
POST /api/v1/requests/{id}/start_search
Response: 202 { "task_id":"uuid" }
4) CRUD поставщиков
GET /api/v1/suppliers?filter=...
POST /api/v1/suppliers {name, inn,...}
5) Подтверждение
POST /api/v1/requests/{id}/approve { chosen_quote_id } → trig. export.
6) Webhook / Export
Webhook в 1С: POST /integration/1c/request_result {request_id, supplier, price, files[]}
11. Логика ограничений по нагрузке (capacity plan)
●
●
●
●
Месячный лимит: 1000 заявок.
Среднее: 34/день.
Планирование ресурсов (prod VPS) для нормальной работы и буфера: для заявок до 1000/мес достаточно: 4 vCPU, 8
GB RAM, 100 GB NVMe (как в черновике). При необходимости — масштаб на чтение/worker-ноды.
Очереди задач (Redis + Celery/RQ). Максимум параллельных worker'ов — настроить так, чтобы одновременно
обрабатывалось не более 10 задач парсинга/поиска, чтобы не превысить лимит API и не перегружать систему.
12. Мониторинг, логирование и алерты
●
●
Метрики: requests_per_minute, avg_parse_time, failed_parses, queue_length, disk_usage, s3_errors.
Alerts:
●
○
○
○
queue_length > 20 → warn;
failed_parses > 5% за 1ч → critical;
disk_usage > 80% → warn.
Логи: структурированные JSON-логи в ELK/Graylog; хранение логов минимум 90 дней.
13. Безопасность и соответствие
●
●
●
●

●
●
●
Бэкап БД: ежедневный дамп, 14 дней ротация.
S3 (MinIO): versioning + ежедневный snapshot, 30 дней хранения стандартно.
Disaster recovery: RPO = 24 ч, RTO ≤ 4 ч для критических сервисов.
15. Тестирование и приёмка
Критерии приёмки (расширено):
1. 2. 3. 4. 5. Полный цикл заявки (import → search → quotes → approve → export) работает без ошибок на тестовых данных при
нагрузке 34 заявок/день.
Получено ≥3 корректно распарсенных КП в тестовом наборе (если доступно).
Парсинг для заданного набора тестовых PDF выполняется ≤ 10 с.
UI отображает чат-логи и таблицу сравнения корректно.
Экспорт в Excel совпадает с шаблоном 1С (проверяется интеграционной командой).
Тест-сценарии (пример):
●
●
●
TC-001: импорт xlsx с 3 позициями → ожидание валидного request_id.
TC-002: запуск поиска → 5 найденных поставщиков → отправка запросов → приход 3 КП (pdf) → проверка парсинга.
TC-003: отказ парсера: соответствующее поле помечено needs_manual_review.
16. План работ и контрольные точки (MVP, 6 недель)
(Развёрнутый план по неделям, на базе черновика с деталями)
Неделя 1 — Подготовка
●
Инициализация репозитория, CI/CD, инфраструктура, схема БД, шаблоны API.
Неделя 2 — UI и импорт
●
Реализовать UI загрузки, импорт XLSX, валидация, CRUD поставщиков.
Неделя 3 — Поиск / Контакты / Рассылка
●
Микросервис поиска, contact crawler, интеграция WhatsApp Web JS (MVP), очередь отправки.
Неделя 4 — AI-парсер и сравнение
●
AI-парсер PDF, таблица сравнения, отображение диалогов.
Неделя 5 — Согласование / Экспорт
●
UI согласования, экспорт approved Excel, отчетность.
Неделя 6 — Тестирование и деплой
●
Полное тестирование (функциональное + нагрузочное до 1000 заявок/мес), фиксы, деплой.
17. Критерии качества и SLA
●
●
●
Гарантия: 90 календарных дней (как в исходнике).
SLA: реакция на критическую ошибку ≤ 4 ч, исправление ≤ 24 ч.
Поддержка: базовый бесплатный период + платные тарифы (например, 7 000 тг/час для доработок по черновику).
18. Документация, обучение и передача
●
●
Доставка: Postman коллекция, OpenAPI спецификация, ER-диаграмма, инструкция по развертыванию (prod/dev),
руководство пользователя (1С → import, согласование, экспорт).
Обучение: 1-дневный онлайн-тренинг для пользователей + запись видео.