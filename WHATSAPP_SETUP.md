# Настройка WhatsApp Web JS

## Обзор

Этот проект использует библиотеку `whatsapp-web.js` для интеграции с WhatsApp Web. Библиотека позволяет отправлять сообщения через WhatsApp программно.

## Основные исправления

### 1. Проблемы с сохранением сессии

**Исправлено:**
- ✅ Добавлен уникальный `clientId` для LocalAuth
- ✅ Исправлен путь к папке сессии (`./wwebjs_auth` вместо `./.wwebjs_auth`)
- ✅ Добавлена проверка существования сохраненной сессии
- ✅ Улучшена обработка событий аутентификации

### 2. Проблемы с загрузкой

**Исправлено:**
- ✅ Убран жестко заданный путь к Chrome (теперь Puppeteer сам находит браузер)
- ✅ Добавлены дополнительные аргументы для Puppeteer
- ✅ Увеличен таймаут до 60 секунд
- ✅ Добавлен webVersionCache для стабильной работы
- ✅ Добавлено событие `loading_screen` для отслеживания прогресса

### 3. Улучшения API

**Добавлено:**
- ✅ Роут `/api/whatsapp/qr` для получения QR кода
- ✅ Опция очистки сессии в `/api/whatsapp/disconnect`
- ✅ Метод `clearSession()` для полной очистки данных сессии
- ✅ Улучшенная обработка ошибок

## API Endpoints

### POST /api/whatsapp/init
Инициализирует WhatsApp клиент

### GET /api/whatsapp/status  
Получает текущий статус подключения

### GET /api/whatsapp/qr
Получает QR код для сканирования (доступен только когда статус = 'qr_ready')

### POST /api/whatsapp/disconnect
Отключает клиент
```json
{
  "clearSession": true  // опционально - очистить сохраненную сессию
}
```

### POST /api/whatsapp/send
Отправляет сообщение
```json
{
  "phoneNumber": "77001234567",
  "message": "Привет!"
}
```

## Тестирование

Запустите тестовый скрипт для проверки работы:

```bash
npm run tsx scripts/test-whatsapp.ts
```

## Статусы подключения

- `disconnected` - Отключен
- `connecting` - Подключается  
- `qr_ready` - QR код готов для сканирования
- `authenticated` - Аутентифицирован
- `ready` - Готов к работе
- `error` - Ошибка

## Процесс подключения

1. **Первое подключение:**
   - Вызовите `/api/whatsapp/init`
   - Дождитесь статуса `qr_ready`
   - Получите QR код через `/api/whatsapp/qr`
   - Отсканируйте QR код в WhatsApp на телефоне
   - Дождитесь статуса `ready`

2. **Последующие подключения:**
   - Если сессия сохранена, клиент автоматически подключится
   - Статус изменится с `connecting` на `ready`
   - QR код не потребуется

## Структура файлов

```
wwebjs_auth/                    # Папка с данными сессии
├── session-whatsapp-client-session/  # Данные конкретной сессии
└── ...
```

## Устранение неполадок

### Сессия не сохраняется
1. Проверьте права доступа к папке `wwebjs_auth`
2. Убедитесь, что процесс не завершается принудительно
3. Попробуйте очистить сессию и переподключиться

### Не загружается WhatsApp Web
1. Проверьте подключение к интернету
2. Убедитесь, что Chrome/Chromium установлен
3. Проверьте логи на наличие ошибок Puppeteer

### QR код не появляется
1. Убедитесь, что статус = `qr_ready`
2. Проверьте, что предыдущая сессия полностью очищена
3. Попробуйте переинициализировать клиент

## Логи

Все важные события логируются с эмодзи для удобства:
- 🚀 Инициализация
- 📱 QR код
- ✅ Успешные операции  
- ❌ Ошибки
- 🔄 Процессы загрузки
- 🔌 Подключение/отключение
